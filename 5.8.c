#include<stdio.h>

int main(){
    int x, y, z;
    printf("Enter a 24-hour time: ");
    scanf("%d:%d", &x, &y);
    z =x*60 + y;
    if(480<=x<=531){   
        printf("Closest departune time is 8:00 a.m");
    }
    else if (532<=x<=)
    }
    return 0;
}